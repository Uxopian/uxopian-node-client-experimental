Find every FlowerDocs document of class [(${classId})]: build a class criterion, wrap it in an AND clause, then run the search.

Return ONLY a JSON array of the document ids (strings), without duplicates — no markdown, no explanation.
